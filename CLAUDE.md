# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**bigboss** is a Claude Code agent skill for setting up observability in multi-agent applications.

## Tech Stack

- **Language:** Python
- **Linting/formatting:** ruff (inferred from `.gitignore`)
- **Type checking:** mypy (inferred from `.gitignore`)
- **Testing:** pytest (inferred from `.gitignore`)

> This repository is in early setup. Update this file with build commands, test commands, and architecture notes once the project structure is established.
