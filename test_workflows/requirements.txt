# --- Agent frameworks ---
crewai>=0.80
langgraph>=0.2.50
langchain-core>=0.3

# --- LLM client (direct Azure + shared service) ---
openai>=1.40

# --- Shared LLM service ---
fastapi>=0.110
uvicorn>=0.29

# --- MCP server + client ---
mcp>=1.2

# --- HTTP + environment ---
httpx>=0.27
python-dotenv>=1.0
