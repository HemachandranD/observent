---
description: Sets up observability for multi-agent Python apps (Arize Phoenix / Langfuse / SigNoz / Elastic APM / LangSmith) using a spec-driven lifecycle (spec → plan → tasks → implement) with project-local persistent state in .observent/. Invoke when the user asks to add tracing, monitoring, telemetry, or LLM monitoring to their agent app, or mentions OpenTelemetry, OpenInference, span hierarchy, or agent handoff visibility.
globs:
  - "**/*.py"
alwaysApply: false
---

# observent — Multi-Agent Observability (Spec-Driven)

This rule is a thin pointer. The **single source of truth** for the observent
workflow is the skill, installed on this machine. Auto-attached to Python files
so it stays out of the way for non-Python work.

**Before doing anything**, read the full workflow and follow it exactly:

- **Full workflow:** `${OBSERVENT_HOME}/SKILL.md`
- **Canonical artifact schema:** `${OBSERVENT_HOME}/references/spec_schema.md`
- **Framework × backend matrix:** `${OBSERVENT_HOME}/references/matrix.md`

The condensed cross-tool summary also lives in this project's root `AGENTS.md`
when present. Do not reimplement the workflow from memory — read `SKILL.md`.
